# Copy to apps/dashboard/.env (optional).
# County planner console — Phase 2 Wk 7–8 ward metrics + LPDP download.
# Leave VITE_API_BASE empty when using `npm run dev` (proxies /api → :3001).

# VITE_API_BASE=http://localhost:3001
